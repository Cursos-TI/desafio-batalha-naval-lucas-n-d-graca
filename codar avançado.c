#include <stdio.h>
#define LINHA 10
#define COLUNA  10
void imprimirTabuleiro(){
    int matriz[LINHA][COLUNA];
    for (int i = 0; i < LINHA; i++){
        for (int j = 0; j < COLUNA; j++){
        soma++;
        matriz[i][j] = soma;
        printf(" %d ", matriz[i][j]);
        }
    }
}
int main(){
    
    
    int alvo = 3 ;
    int soma = 0;
    imprimirTabuleiro();

    
        
        if (matriz = 23){
            printf(" %d ", alvo);
        }else{
            printf(" %d ", soma);
        
        }
    printf("\n");
    
       
    
    
    
    return 0;
}